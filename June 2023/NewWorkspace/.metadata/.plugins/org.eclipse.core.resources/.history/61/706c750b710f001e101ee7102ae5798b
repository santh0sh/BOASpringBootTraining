package com.service;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.dao.UserDAO;

@Service
public class UserService {
	
	@Autowired
	private UserDAO userdao;
	
	List<UserService> a1 = new ArrayList<UserService>();
	public boolean loginValid(UserService user) {
		if(user.getUname().equals("admin") && user.getPass().equals("admin123")) {
			return true;
		}
		return false;
	}
	
	public String registration(UserService user) {
		userdao.save(user);
		System.out.println(a1);
		return "User registered";
	}
	
	public List<UserService> loadUser() {
		userdao.findAll();
		System.out.println(a1);
		return a1;
	}
	
	public boolean findUser(String name) {
		for(UserService user: a1) {
			if(user.getUname().equals(name)) {
				return true;
			}
		}
		return false;
	}
	
	public boolean deleteUser(String name) {
		for(UserService user: a1) {
			if(user.getUname().equals(name)) {
				a1.remove(user);
				return true;
			}
		}
		return false;
	}
	
	public void updateUser(String name, UserService user1) {
		for(UserService user: a1) {
			if(user.getUname().equals(name)) {
				a1.set(a1.indexOf(user), user1);
			}
		}
	}
}
