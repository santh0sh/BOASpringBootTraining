
Trainer :-
Amarjeet

Important  URLS:-

Create a Spring template Maven project from https://start.spring.io/


Tutor Git hub :- https://github.com/amarjeet-arora/boajune

Tutor Notepad notes :- https://notepad.pw/838172zc


sudo apt-get update
sudo apt-get install docker.io
sudo docker -v 

sudo docker images - to list images

sudo docker ps -- to list all containers
sudo docker run --name mysql-standalone -e MYSQL_ROOT_PASSWORD=password -e MYSQL_DATABASE=test -e MYSQL_USER=sa -e MYSQL_PASSWORD=password -d mysql:5.6
sudo docker ps -a -- to listrunning containers

sudo docker run --name mysql-standalone -e MYSQL_ROOT_PASSWORD=password -e MYSQL_DATABASE=test -e MYSQL_USER=sa -e MYSQL_PASSWORD=password -d mysql:5.6

